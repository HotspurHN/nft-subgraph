{
  "name": "mixort-nft",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ mixort-nft",
    "create-local": "graph create --node http://localhost:8020/ mixort-nft",
    "remove-local": "graph remove --node http://localhost:8020/ mixort-nft",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 mixort-nft",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.33.0",
    "@graphprotocol/graph-ts": "0.27.0"
  },
  "devDependencies": { "matchstick-as": "0.5.0" }
}
